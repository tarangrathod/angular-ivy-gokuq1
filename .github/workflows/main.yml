# name: 'tag'
# on:
#   push:
#     branches:
#       - main
# jobs:
#   tag:
#     runs-on: ubuntu-latest
#     # run: | 
#     #   t=$(git describe --tags `git rev-list --tags --max-count=1`)
#     #   echo $t
#     steps:
#     - uses: actions/checkout@v2
#     - uses: christophebedard/tag-version-commit@v1
#       id: tag-version-commit
#       with:
#         token: ${{ secrets.TOKEN1 }}
#         version_regex: '([0-9]+\.[0-9]+\.[0-9]+)'
#         version_tag_prefix: 'V'
#         check_entire_commit_message: true
#     - run: echo "${{ steps.tag-version-commit.outputs.tag }}"
 
# - name: Create Git tag from commit message
#   shell: "bash --noprofile --norc {0}"
#   - run: |
#     tag=$(git log -1 --pretty=%B | grep -o 'VERSION=[0-9]\.[0-9]\.[0-9]' | cut -d= -f2)
#     git tag $tag
#     git push --tags
#   env:
#     GITHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
